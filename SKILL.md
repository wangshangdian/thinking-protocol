---
name: thinking-protocol
description: >
  跨项目的"决策卫生"协议。用于任何需要"先定方向再动手"的工作：需求模糊需要澄清、
  要判断某个想法/方案对不对、要设计架构或技术选型、要评估可行性与风险、要裁决一条路该不该走、
  要把项目用文件组织起来、要把新想法收集起来不打断主线、要判断某个动作会不会烧掉未来的选项。
  核心是按住模型的默认失效模式（收到模糊需求就自动补全、然后高效地做错方向）。
  当用户说"分析一下我的想法""这个方案行不行""帮我设计""要怎么做""评估一下""这个思路靠谱吗"
  "先别做，想想""这值得做吗""会不会后悔"，
  或需求描述不完整、出现多条实现路线、涉及不可逆成本、需要引用学术/工业界成熟方法时使用。
  也适用于用户要求把自创方法/直觉/经验对照正规方法论、要求建立项目文档与思路收集机制、
  或涉及复利资产化/长期主义/不可逆决策（为未来保留选项、保留余量）的场景。
  不适用于单步可完成、纯信息查询或结果完全可逆无成本的任务。
agent_created: true
---

# Thinking Protocol · 决策卫生协议

## 0. 解决的问题

**方向风险 > 执行风险。**

模型的默认失效模式是「顺从式幻觉执行」：收到模糊需求 → 自动补全成一个具体版本 → 高效地做出来。
看起来非常努力，实际上方向已经歪了。本协议用一组检查项把这个默认行为按住。

第二层原理：**在信息不完备时，先花低成本买信息，再花高成本执行。**
（学术对应：Value of Information / EVPI，见 `references/04`）

第三层原理：**不可逆动作杀死未来的选项，而未来需要选项。**
（学术对应：Real Options，见 `references/07`）

## 1. 启用门槛（先分档，别仪式化）

| 档 | 条件 | 走哪些 |
|---|---|---|
| 轻 | 单步可完成 / 纯信息查询 / 结果可逆无成本 | 直接答，不铺流程（**答案本身**就是交付物） |
| 标准 | 多步任务、要写代码或产出文档 | P1 → P5（可精简） |
| 重 | 长期项目、多轮迭代、涉及不可逆成本（资金 / 外发 / 删除 / 实体动作） | 全部 + P6 建文档骨架与收件箱 |

分档问句：**「这件事做完之后还会不会继续改它？」** 会 → 至少标准档。

⚠️ 把重型协议用在轻量任务上是**负资产**——过度仪式化会导致整个技能被弃用。

## 2. 八项协议

> **一屏版（读到这儿就够用）**——下面这张表是全部索引；轻档任务到此为止，不必往下读。
> 常看的只有两列：「什么时候用」决定要不要往下读，「深读」决定读哪份 reference。

| | 一句话 | 什么时候用 | 深读 |
|---|---|---|---|
| P1 | 补信息的责任在**模型**，不在用户 | 需求模糊、准备提问之前 | `01` |
| P2 | 先判对错再给方案；**两级对照**防假发现 | 评价主张、查证"我说过没有" | `02` |
| P3 | 找**硬约束**（不是找"本质"）；定完方向要回验可行性 | 设计架构、技术选型、定路线 | `03` |
| P4 | 延伸最多 3 条，**命中绑定约束的排最前** | 用户要"重要延伸" | `04` |
| P5 | 自创方法先去找**官方名字** | 用户提野路子 / 自造指标 | `04` |
| P6 | 子任务靠**文件**传递，不靠记忆；版本控制是底座 | 建文档骨架、写开工包、回写结论 | `05` |
| P7 | 边界由**不可逆性**定义，不由难易定义 | 判断值不值得做、动密钥 / 外发前 | `07` |
| P8 | 「**怎么问**」比「问谁」重要一个数量级 | 换模型、切强模型复核时 | `08` |

### P1 澄清：把「补全信息」的责任接过来

核心认知：**需求说得不清楚，不是让用户自己想清楚，而是让模型承担提供信息的责任。**

1. 先自己查、自己推、自己给默认答案，**然后**再问。
2. 只问**不可推断的岔路**——会改变架构、方向或成本量级的。
3. 每次提问**必须自带默认答案**：「我打算这么做，你只需否决」。用户的回应成本最低为一个「对」。
4. 一次问完，不挤牙膏。一批最多 4 个问题。
5. **轮次预算 2 轮**。两轮后仍不明确 → 停止追问，转「最小可逆探针」（做最便宜的可逆实验去买信息）。
   依据：**追问序列不收敛**是已知失效模式（坑 #39）。
6. 六要素清单：**目标 / 成功判据 / 范围边界（不做什么）/ 硬约束 / 交付形态 / 失效条件**。
   六项都有落点即可开工——不需要主观「85% 信心」，那个数值不可证伪。

→ 详见 `references/01-澄清与提问.md`

### P2 客观裁定：先判对错，再给方案

收到用户主张后，固定输出：**判定 ✅/🟡/❌ + 哪部分成立 + 哪部分不成立 + 依据 + 置信度**。

三条纪律：
1. **切开复合主张**——一句话常含几个独立命题，逐个判，不整体打分。
2. **两级对照**——任何「发现了结构」的结论，必须与随机对照比（循环位移 + 打乱保分布）。
   只跟原始基线比 = 假发现。
3. **区分「文献真」与「对本项目真」**——上游结论正确 ≠ 应用到这里正确。

双向防偏：既防**讨好式同意**，也防**为反对而反对**。标准是**证据**，不是立场。
用户已明确授权「不客气」。不确定必须标注，不得把不确定说成确定。

**也适用于查证"某条原则我到底有没有说过"**——先去文件和历史里查，再回答"有/没有/有但不在你以为的位置"。

→ 详见 `references/02-客观裁定.md`

### P3 自顶向下 + 第一性原理

顺序硬约束：**目标与约束 → 方法 → 执行**。反了就是屎山。

「原理的原理」的可操作化——**不是找「本质」，是找硬约束**：

| 类型 | 判定问句 | 举例 |
|---|---|---|
| 硬约束 | 违反它会失败 | 数学 / 物理 / 账户规则 / 样本量 / 成本 / 法律 |
| 软约束 | 违反它只是不主流 | 行业习惯、"别人都这么干" |

上溯三问：① 违反这条会怎样？② 那个后果由什么决定？③ 拆到不能再拆，剩下什么？

**双向对接**：自顶向下定完方向，必须自底向上回验可行性（数据 / 样本量 / 算力 / 时间 / 成本）。
只朝下看会得到漂亮但做不到的方案。

输出必须包含：**绑定约束清单**（真正卡住我们的那几条）。
反例：空洞第一性原理（「本质是为了赚钱」）零操作性 → 必须落到"因此该做什么 / 不该做什么"。

→ 详见 `references/03-第一性原理与自顶向下.md`

### P4 延伸方向（限额）

用户明确要求「重要延伸提一嘴」。执行规则：

- 硬限额：**最多 3 条，每条 ≤ 2 行**。
- 排序：重要性 × 可执行性；**命中绑定约束的排最前**。
- 格式：一句话 + 为什么重要 + 最小下一步。
- 「提一嘴」= 不展开论证，等追问。
- 禁止堆成愿望清单。

→ 详见 `references/04-延伸与正规军对照库.md`

### P5 正规军对照（常驻，非可选）

用户提出自创方法 / 指标 / 直觉时，**先去找官方版本**，而不是直接实现野路子。三问：

1. 学术界有没有**正式名字**？（找到名字 = 找到全部文献）
2. 有没有成熟库 / 实现？（先找轮子）
3. 它已知的**失效条件**是什么？（正规军的边界往往正是野路子的坑）

输出：正式名称 + 1–2 关键出处 + 与野路子的差异 + 是否值得换。
反模式：为显专业堆术语——要么给名字和出处，要么不说。

→ 对照表见 `references/04-延伸与正规军对照库.md`

### P6 复利资产化 + 文件化工程

铁律：**子任务不靠记忆传递，靠文件传递。**

```
docs/
  00-项目章程.md          宪法层：目标 / 边界 / 底层思维。单一事实源，少改，改要记日期
  01-思路评估.md          成熟想法逐条裁定
  02-子任务登记.md         状态机：待启动 / 研究中 / 已闭环 / ⛔作废
  03-思路收件箱.md         ⭐ 新想法先丢这里，追加式，不打断主线
  04-轮子清单.md          先找轮子
  05-方法论与教训.md       复利资产：M 编号通则 + 坑编号
  06+ 编号递增-主题.md      领域文件，编号只增不减
  子任务/SC-XX-短名.md      自包含开工包（6 段）
```

七条机制：

0. ⭐ **资产化判定**：每次实质输出前问一句「**三个月后还能用吗？**」
   能用 → 结构化落盘（模块 / 数据集 / 结论+失效条件 / 模板 / 通则）。
   不能用 → **明确标记为一次性消耗**，不要假装它是资产。
   （细则见 `references/07`）
1. **收件箱三动作**：分诊（成熟 → 01 / 需研究 → 02 开子任务 / 太小 → 攒着）+ 更新去向 + 勾选 `[x]`。
2. **回写三处**：登记表状态行 + 开工包 `## 结论` + 方法论与教训。**不回写 = 这次研究的钱白花。**
3. **单一事实源**：哲学 / 规格只写一处，其他地方只引用不复述。
4. **开工包自包含**：新对话不看任何其他文件也能开工；末尾放「★ 复制区」可直接粘贴。
5. **编号只增不减**：作废的课题写清**为什么废**，不删——它告诉你此路不通。
6. ⭐ **版本控制是文件化的底座，不是可选项**（2026-09-18 实证补入）。
   **云同步 ≠ 版本控制**：OneDrive / iCloud / 坚果云的目标是"多端一致"，不是"保留每次变更"。
   它冲突时生成 `文件名-计算机名.md` **副本**让你自己收拾，并且**没有任何机制告诉你主文件坏了**
   —— 实测过：主文件丢掉整节内容（只在冲突副本里）、文件里出现 1 个非法字节，
   两者都能"正常打开"，沉默数天。
   ⇒ 两条底线：**① 建库 + 基线提交，然后才允许删改**（删错了 `git checkout <sha> -- <path>` 就能回滚）；
   **② 每次实质改动后 `git log --oneline` 能看见**。
   反过来，**冲突副本本身是信号**：出现 `*-<主机名>.md` 就说明"两端各自改过"，
   处理顺序是「**先比对内容 → 抢救独有内容进主文件 → 再删副本**」，**绝不能直接删**。
   若两台机主机名相同，冲突副本会**同名互覆**，数量与实际冲突次数不成比例 → 先改名。

认知负荷：用文件外置知识，不要用上下文硬扛。
新项目可用 `scripts/init_project_docs.py` 一键铺好骨架。

→ 详见 `references/05-文件化工程与思路收件箱.md`

### P7 支援未来（跨期可逆性）

一句话：**判断任何动作，不只看它现在产出什么，还要看它会不会烧掉未来的选项。**

三问：
1. 它会**不可逆地消耗**什么？（资金 / 数据 / 信任 / 时间 / 结构 / 身体）
2. **未来的我需要什么**？我现在的做法是给它留了，还是堵了？
3. 一年后的我看到这个决定，会觉得**被支援**，还是被埋坑？

四个机制：
- **保留余量**：任何不可逆投入 ≤ 可承受范围（满负荷系统无弹性）。
- **可逆优先**：两条路都能到达时，选可逆的那条。
- **消耗转资产**：把一次性支出改造成可复用的（与 P6 接上）。
- 🚨 **凭据与隐私的最小暴露面**（09-18 补）：密钥一律放**云盘同步范围之外 + `.gitignore` 之内**；
  `.gitignore` **先行**、而非事后补；外发（推仓库 / 发文件 / 贴对话）前**扫一遍**。
  ⭐ 两个反直觉：「只读密钥也是密钥」（降损失上限，不改变不可逆性质）；「私有仓库 ≠ 安全」
  （默认位置永远是**库外**，不是"库内但私有"）。
  ⚠️ 已泄露**不要试图清理，直接轮换**——清理历史只能减少二次暴露，不能撤回已发生的暴露。

⭐ **它是「授权分级」的第一性原理**：为什么边界由不可逆性定义？
因为不可逆动作杀死未来的选项，而未来需要选项。
**推论：「难」不是暂停的理由，「易」也不是动手的理由——只有「不可逆」才是。**

→ 详见 `references/07-复利与支援未来.md`

### P8 模型分工与纠偏（元层协议）

一句话：**该用谁来想，和怎么问它，比"哪个模型最强"重要。**

它防三个具体失效：
1. **一个模型既做又审** → 自己审自己 = 没审。审查必须**跨厂商**（同厂商同代际盲区高度相关）。
2. **拿榜单当结论** → 榜单测的东西可能和你要的不是一回事。至少分四个轴看：
   人类偏好榜（偏讨喜，不偏正确）/ 能力分榜（口径不同，多为无工具态）/ **有工具态 vs 无工具态**（同一模型可以完全不是一回事）/ 聚合站（**只当索引，不当依据**）。
3. **结果好得不像真的就直接采信** → 这是**唯一应硬编码进流程的触发器**，必须复核。

五条操作规则（核心两条）：
- ⭐ **怎么问比问谁重要一个数量级**：给它「你的原话主张 + 已知硬约束 + **要求它反驳你**」。
- ⭐⭐ **强模型的意见是输入，不是结论**：与 P2 同构，「上游说得对」≠「对本项目对」。
  它知道得更多 ⇒ 永远能找出新的"遗漏" ⇒ **这个序列不收敛**（见 `references/06 §四 停止规则`）。
  ⇒ 接到"换成 X"的指令，先跑 P3 的绑定约束清单：**点不出它缓解了哪条 B 编号，就不进路线**。

必切强模型的四种情形：① 定方向/定架构 ② 结果好得不像真的 ③ 审查已完成的东西 ④ 涉及不可逆成本。
⭐ **成本不是决策变量**：一次错误方向的代价远高于多问一次的 API 费用。

⚠️ 结论必须记录**来源**：哪个模型 + 什么时间 + 是否带工具 —— 否则日后无法复核。

→ 详见 `references/08-模型分工与纠偏.md`

## 3. 输出契约（每条实质结论都要有）

1. **判定**（✅ / 🟡 / ❌）
2. **依据**（出处 / 实测 / 推理链）
3. **置信度** + 明确标出不确定处
4. **失效条件**：什么情况下这条结论作废
5. **落文件位置**（重档任务）
6. **授权分级**（见下表）——理由：不可逆动作会杀死未来的选项（P7）

| 影响半径 \ 可逆性 | 可逆 | 不可逆 |
|---|---|---|
| 仅本工作区 / 自己 | 直接做，事后报 | 说明后做，留备份 |
| 影响外部 / 他人 | 先确认 | **必须先确认 + 备份** |

边界由**不可逆性**定义，不由"难不难"定义。
按 P6 再问一句：这个输出**三个月后还能用吗**？不能，就明确标成一次性消耗。

## 4. 反模式（违反即失败）

- 用重型流程处理轻量任务（仪式化）
- 追问不收敛 / 问能自己查到的事
- 空洞第一性原理（正确但无操作性）
- 延伸清单超过 3 条
- 讨好式同意 或 为反对而反对
- 把「文献正确」当成「对本项目正确」
- 只跟基线对照就宣布发现
- 结论不带失效条件
- 把「写了文档」当成资产化（文档不能执行、没人读，也是成本）
- 用可逆的动作拖时间，或对不可逆的动作图省事
- 同一个模型既做又审；拿聚合排名当架构依据（见 P8）

→ 详见 `references/06-反模式与失效条件.md`

## 5. 参考文件索引

| 文件 | 何时读 |
|---|---|
| `references/01-澄清与提问.md` | 需求模糊、准备提问前 |
| `references/02-客观裁定.md` | 要评价用户主张、判断真假、查证"我说过没有"时 |
| `references/03-第一性原理与自顶向下.md` | 设计方案、定架构、找约束时 |
| `references/04-延伸与正规军对照库.md` | 提延伸方向、对照学术方法时 |
| `references/05-文件化工程与思路收件箱.md` | 建文档骨架、写开工包、回写结论时 |
| `references/06-反模式与失效条件.md` | 怀疑自己走偏时 |
| `references/07-复利与支援未来.md` | 判断"这值不值得做"、资产化、不可逆决策、处理密钥与凭据时 |
| `references/08-模型分工与纠偏.md` | 要换模型/切强模型/让另一个模型复核时；结果好得不像真的时 |
| `scripts/init_project_docs.py` | 新项目一键铺文档骨架 |
